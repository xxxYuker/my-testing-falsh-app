name: Deploy to DigitalOcean

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # 1. 进入项目目录
            cd /root/web项目
            
            # --- 🛡️ 任务 C：自动备份数据库 (防止手滑) ---
            # 如果数据库存在，就复制一份，名字加上时间戳 (例如 data.db_202310121230.bak)
            if [ -f "data.db" ]; then
              cp data.db "data.db_$(date +%Y%m%d%H%M%S).bak"
              echo "✅ Database backed up successfully."
            fi

            # 2. 拉取最新代码
            git pull origin main

            # --- 📦 任务 A：自动安装依赖 ---
            # 直接使用系统 pip 安装 (如果 requirements.txt 没变，这步会很快跳过)
            pip install -r requirements.txt

            # 3. 重启 Flask 服务
            sudo systemctl restart my_flask.service
